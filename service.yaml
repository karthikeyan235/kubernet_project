apiVersion: v1
kind: Service

metadata:
  name: python-flask-service

spec:
  type: NodePort

  selector:
    app: python-flask

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
